local polar = require(game.ReplicatedStorage.polarUI)

polar.New("Frame", "dark", {
    Parent = script.Parent,
    Name = "Frame",
    Size = UDim2.new(0, 200, 0, 200),
    Position = UDim2.new(0, 0, 0, 0)
})